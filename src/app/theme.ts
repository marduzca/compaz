import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
      light: '#4794ff',
      dark: '#004BA0',
      contrastText: '#ffffff',
    },
  },
});

export default theme;
