import React from 'react';
import styles from './PageContent.module.css';

const PageContent: React.FC = () => (
  <div className={styles.content}>This is the page content</div>
);

export default PageContent;
